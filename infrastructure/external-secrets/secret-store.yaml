apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore # All namespace can use this secret store
metadata:
  name: global-vault
spec:
  provider:
    kubernetes:
      server:
        url: "https://kubernetes.default.svc"
      remoteNamespace: vault-sim